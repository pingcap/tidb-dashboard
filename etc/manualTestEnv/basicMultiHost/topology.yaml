global:
  user: tidb
  deploy_dir: tidb-deploy
  data_dir: tidb-data

server_configs:
  pd:
    replication.enable-placement-rules: true

pd_servers:
  - host: 10.0.1.11

tikv_servers:
  - host: 10.0.1.12

tidb_servers:
  - host: 10.0.1.13

tiflash_servers:
  - host: 10.0.1.14

grafana_servers:
  - host: 10.0.1.11

monitoring_servers:
  - host: 10.0.1.11

alertmanager_servers:
  - host: 10.0.1.11
