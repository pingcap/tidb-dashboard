module github.com/pingcap/tidb-dashboard/ui

// a hack to ignore this directory in go commands
// note: this is still an path that can be imported, as the source may be need in
// some other places, like PD.

go 1.13
